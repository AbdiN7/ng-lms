import { LmsNameSortPipe } from './lms-name-sort.pipe';

describe('LmsNameSortPipe', () => {
  it('create an instance', () => {
    const pipe = new LmsNameSortPipe();
    expect(pipe).toBeTruthy();
  });
});
