import { LmsBranchSortPipe } from './lms-branch-sort.pipe';

describe('LmsBranchSortPipe', () => {
  it('create an instance', () => {
    const pipe = new LmsBranchSortPipe();
    expect(pipe).toBeTruthy();
  });
});
